{
  "name": "pact-message-example",
  "version": "1.0.0",
  "description": "Example async message application",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "clean": "if [ -d 'pacts' ]; then rm -rf pacts; fi",
    "test": "npm run test:consumer && npm run test:publish && npm run test:producer",
    "test:producer": "nyc --check-coverage --reporter=html --reporter=text-summary mocha producer/*.spec.ts",
    "test:consumer": "nyc --check-coverage --reporter=html --reporter=text-summary mocha consumer/*.spec.ts",
    "test:publish": "node publish.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^2.2.41",
    "axios": "^0.14.0",
    "chai": "^3.5.0",
    "mocha": "^3.5.3",
    "nyc": "^11.6.0"
  }
}
