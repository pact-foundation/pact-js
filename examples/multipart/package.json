{
  "name": "pact-multipart-example",
  "version": "1.0.0",
  "description": "File upload and multipart form data with Pact",
  "private": true,
  "scripts": {
    "test": "npm run test:consumer && npm run test:provider",
    "test:consumer": "vitest run consumer.test.ts",
    "test:provider": "vitest run provider.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@pact-foundation/pact": "file:../..",
    "@types/express": "^5.0.0",
    "@types/multer": "^1.4.12",
    "@types/node": "^20.0.0",
    "axios": "^1.12.0",
    "express": "^5.1.0",
    "form-data": "^4.0.0",
    "multer": "^1.4.5-lts.1",
    "typescript": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
